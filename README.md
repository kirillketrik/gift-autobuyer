# 📦 Telegram Gift Autobuyer — Документация

Этот проект позволяет **автоматически покупать подарки в Telegram** по заданным вами фильтрам и параметрам.

---

## 📥 Глава 1: Установка

### Шаг 1. Получение ключей

1. Перейдите на [https://my.telegram.org](https://my.telegram.org), войдите в свой Telegram-аккаунт.
2. Создайте новое приложение и получите:

    * **API\_ID**
    * **API\_HASH**
3. Создайте Telegram-бота через [@BotFather](https://t.me/BotFather) и получите:

    * **BOT\_TOKEN**

---

### Шаг 2. Настройка `.env` файла

Создайте файл `.env` на основе шаблона `.env-dist` и укажите значения:

```env
API_ID=ваш_api_id
API_HASH=ваш_api_hash
BOT_TOKEN=ваш_bot_token
PAUSE_AFTER_GET=задержка_между_попытками_автопокупки_в_секундах
PAUSE_AFTER_BUY=задержка_после_покупки_каждого_подарка_в_секундах
```

#### Описание переменных:

* **API\_ID / API\_HASH** — ключи вашего Telegram-приложения.
* **BOT\_TOKEN** — токен вашего Telegram-бота. Через него вы будете получать уведомления (о балансе, ошибках и т.д.).
* **PAUSE\_AFTER\_GET** — задержка между получениями списка подарков (рекомендуется > 10 сек).
* **PAUSE\_AFTER\_BUY** — задержка после каждой покупки (например, 1 секунда).

---

### Шаг 3. Установка на Linux

1. Перейдите в папку проекта:

   ```bash
   cd telegram-autobuyer
   ```

2. Запустите установочный скрипт:

   ```bash
   sudo bash scripts/install.bash
   ```

3. Дождитесь сообщения:

   ```bash
   [*] Готово, установка успешно завершена!
   ```

---

### Шаг 4. Запуск автоскупщика

1. Убедитесь, что контейнер работает:

   ```bash
   docker ps
   ```

   Пример вывода:

   ```
   CONTAINER ID   IMAGE                      COMMAND            STATUS         NAMES
   dbcacae0e758   telegram-gift-autobuyer    "python main.py"   Up X seconds   telegram-gift-autobuyer
   ```

   **⚠️ Под IMAGE значение должно быть точно таким же, пока оно не такое же не переходите к следующему пункту**

2. Запустите:

   ```bash
   sudo bash scripts/run.bash
   ```

3. Подключение к контейнеру:

   ```
   [*] Container already running. Connecting...
   ```

4. Нажмите `ENTER`, начнётся авторизация:

    * Введите ваш **номер телефона**
    * Код из Telegram
    * Облачный пароль (если включена 2FA)

5. Перейдите в Telegram-бота и отправьте команду `/start`

---

Отлично! Вот обновлённая **Глава 2: Гайд по использованию**, полностью синхронизированная с моделью `GiftFilter` и
логикой интерфейса:

---

## 🛠️ Глава 2: Гайд по использованию

### 1. Управление получателями

1. Нажмите **📝 Редактировать** в разделе **👤 Получатели 👤** в главном меню.
2. Введите username'ы получателей через запятую (например: `user1,user2,mychannel`). Символ `@` указывать не нужно.
3. После ввода система покажет кликабельные валидные username’ы.
4. Убедитесь, что всё правильно, и нажмите **✅ Подтвердить**.
5. Старые получатели будут заменены на новых

---

### 2. Управление фильтрами

Фильтры — это основа логики автопокупки. Они определяют, какие подарки будут куплены, а какие — проигнорированы.
Вы можете создать несколько фильтров с разными условиями. Для начала давай рассмотрим все доступные поля фильтров.

---

#### 2.1. Доступные поля фильтра

| Поле              | Описание и как работает                                                                                                                                                                                                                                                                              | Значение по умолчанию   |
|-------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------|
| `min_supply`      | **Минимальное количество выпущенных подарков.**<br>Покупаются только подарки, у которых осталось не меньше этого количества.<br>Например, `min_supply = 10` — подарки с остатком меньше 10 игнорируются.<br>Если `< 0`, условие не применяется.                                                      | `-1` (не учитывать)     |
| `max_supply`      | **Максимальное количество выпущенных подарков.**<br>Покупаются только подарки, у которых остаток не больше этого значения.<br>Например, `max_supply = 50` — подарки с остатком более 50 игнорируются.<br>Если `< 0`, ограничение отключено.                                                          | `-1` (не учитывать)     |
| `min_price`       | **Минимальная цена подарка (в звёздах).**<br>Покупаются подарки с ценой не меньше этого значения.<br>Например, `min_price = 100` — игнорируются все подарки дешевле 100 звёзд.<br>Если `< 0`, ограничение не применяется.                                                                            | `-1` (не учитывать)     |
| `max_price`       | **Максимальная цена подарка.**<br>Покупаются только подарки с ценой не выше этого значения.<br>Например, `max_price = 500` — игнорируются подарки дороже 500 звёзд.<br>Если `< 0`, условие не учитывается.                                                                                           | `-1` (не учитывать)     |
| `priority`        | **Приоритет применения фильтра.**<br>Чем выше значение, тем раньше применяется фильтр при обработке.<br>Помогает определить порядок применения, если подарок подходит под несколько фильтров.<br>Больше значение = выше приоритет.                                                                   | `0` (средний приоритет) |
| `weight`          | **Доля бюджета, выделяемая на покупки по этому фильтру.**<br>Если `> 0`, фильтр получает часть бюджета пропорционально весу.<br>Формула распределения: `(вес фильтра / сумма весов всех фильтров) × общий бюджет`.<br>Если `<= 0`, фильтр не участвует в распределении бюджета.                      | `0` (не участвует)      |
| `max_buy_count`   | **Максимальное количество покупок одного подарка.**<br>Например, `max_buy_count = 3` — подарок будет куплен не более 3 раз.<br>Если `-1`, ограничение отсутствует (можно покупать сколько угодно раз).                                                                                               | `-1` (без ограничений)  |
| `max_spend_money` | **Максимальная сумма звёзд, которую можно потратить на один подарок.**<br>Например, `max_spend_money = 1000` — ограничит траты на покупку конкретного подарка 1000 звёздами.<br>Если `-1`, ограничение отсутствует.                                                                                  | `-1` (без ограничений)  |
| `ordering`        | **Правило сортировки подарков, которые проходят фильтр.**<br>Возможные значения:<br>- `+price`: сначала самые дешёвые;<br>- `-price`: сначала самые дорогие;<br>- `+supply`: сначала подарки с наименьшим остатком;<br>- `-supply`: сначала подарки с наибольшим остатком.<br>По умолчанию `-price`. | `-price`                |

---

#### 2.2 Упраление фильтрами

##### 2.2.1 Меню

В главном меню бота доступны действия (с действием **👁 Показать** я думаю всё понятно...):

* **➕ Создать** — начать создание нового фильтра.
* **👁 Показать** — просмотреть список уже созданных фильтров.
* **🗑️ Удалить** — удалить один или несколько фильтров.

##### 2.2.2 Создание фильтров

1. Нажмите кнопку **➕ Создать** — бот предложит выбрать способ создания фильтров (например, с помощью ИИ или вручную).
2. Следуйте подсказкам бота, чтобы описать условия для фильтра. Вы можете:
    * Написать запрос простым языком и ИИ сгенирирует фильтры по вашему запросу, примеры показаны в боте и главе 2.2.4.
    * Ввести параметры вручную в формате `name=value`, примеры показаны в боте и главе 2.2.5.
3. Бот сформирует фильтры на основе вашего запроса и покажет их для подтверждения.
4. При необходимости листайте страницы с фильтрами кнопками **⬅️ Назад / ➡️ Далее**.
5. Подтвердите создание фильтров нажатием **✅ Сохранить** или отмените операцию.

##### 2.2.3. Удаление фильтров

1. Посмотрите идентификаторы (ID) фильтров, которые хотите удалить с помощью **👁 Показать**. Пример **🔸 Фильтр #3**,
   ID=3.
2. В меню фильтров нажмите **🗑️ Удалить**.
3. Введите через запятую идентификаторы (ID) фильтров, которые хотите удалить.
4. Подтвердите удаление.

##### 2.2.4 Примеры запросов к ИИ для создания фильтров и результаты.

**👨 Пример запроса №1:**

```text
Хочу покупать подарки с саплаем до 10000, цена от 200 до 1000 звёзд, максимум по 3 штуки каждого.
```

**🤖 Результат:**

```text
🔸 Фильтр #1
 - Цена: 200-1000
 - Саплай: до 10000
 - Макс. кол-во: 3
 - Сортировка: -price
```

**👨 Пример запроса №2:**

```text
Сделай два фильтра: первый — для подарков от 1000 звёзд и выше, с весом бюджета 40%; 
второй — для подарков до 300 звёзд, с весом бюджета 60% и ограничением по количеству 5 штук.
```

**🤖 Результат:**

```text
🔸 Фильтр #1
 - Цена: от 1000
 - Доля бюджета: 40
 - Сортировка: -price

🔸 Фильтр #2
 - Цена: до 300
 - Доля бюджета: 60
 - Макс. кол-во: 5
 - Сортировка: -price
```

#### 2.2.5 Примеры ручного создания фильтров и результаты.

Понял! Вот 6 записей в нужном формате, где **запросы** заданы как `min_price=... max_price=...` и т.п.

---

**👨 Пример запроса №1:**

```text
min_price=1000 weight=40 ordering=-price

max_price=300 weight=60 max_buy_count=5 ordering=-price
```

**🤖 Результат №2:**

```text
🔸 Фильтр #1
 - Цена: от 1000
 - Доля бюджета: 40
 - Сортировка: по убыванию цены

🔸 Фильтр #2
 - Цена: до 300
 - Доля бюджета: 60
 - Макс. кол-во: 5
 - Сортировка: по убыванию цены
```

---

**👨 Пример запроса:**

```text
min_supply=10 max_price=500 max_spend_money=2000 ordering=-price
```

**🤖 Результат:**

```text
🔸 Фильтр #1
 - Кол-во: от 10
 - Цена: до 500
 - Макс. трата: 2000
 - Сортировка: по убыванию цены
```

---

#### 2.2.6. Как работает фильтрация (простыми словами)

Каждый фильтр — это набор условий, которым должен соответствовать подарок, чтобы его можно было купить. Вот как это
работает шаг за шагом:

1. **Остаток (supply)**
   Если вы указали, что хотите только подарки, у которых осталось **не меньше** определённого количества — фильтр будет
   проверять, сколько их осталось.
   Например, если вы задали `min_supply = 10`, а у подарка осталось только 5 — он будет проигнорирован.
   То же самое и с `max_supply` — если вы хотите не больше 50, а их осталось 100 — подарок также отфильтруется.

2. **Цена**
   Вы можете указать минимальную и/или максимальную цену подарков.
   Например:

    * `min_price = 100` — подарки дешевле 100 звёзд не будут рассматриваться.
    * `max_price = 500` — всё, что дороже 500 звёзд, тоже будет исключено.

3. **Если вы не указали какое-то из этих условий (цена или остаток)** (или оставили его как `-1`, что по умолчанию), оно
   **просто не будет
   учитываться**.
   Например, если вы задали только `max_price = 500`, то фильтр примет любой подарок дешевле 500 звёзд, независимо от
   того, сколько их осталось или каков минимум.

4. **Приоритет**
   Фильтры начинают обрабатывать подарки в соответствии со своими приоритетом. Выше приоритет - раньше сработал.
   Итак, допустим наш фильтр сработал первым, так как у него высший приоритет.
   Если мы не указали **max_buy_count**, **max_spend_money** или **weight**,
   фильтр будет стараться распределить все имеющиеся финансы, на все подарки, попадающие под данный фильтр.
   Также важна сортировка, ведь от неё зависит какой подарок будет распределяться фильтром первый.

5. **Доля бюджета (weight)**
   Распределяет бюджет между всем фильтрами, которые имеют вес. Остальные в распределнии не участвуют.
   Если вес, в каком-то фильтре всё весовое распределение бюджета игнорируется.

6. **Неизвестные данные**
   Если у подарка **неизвестна цена или остаток** — он считается неподходящим.

📌 Таким образом, фильтр последовательно проверяет каждый подарок на соответствие указанным ограничениям. Если хотя бы
одно условие нарушено — подарок **не покупается**.
---
